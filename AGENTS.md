# Project preferences

- Use the existing Firebase project configured in `.env` for the application.
- Do not create, restore, seed, or switch the application to a demo database or emulator project. The user explicitly removed the demo setup.
- Do not substitute sample data or synthetic approvals for the real learning configuration.
